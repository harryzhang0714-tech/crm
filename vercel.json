{
  "installCommand": "rm -f pnpm-lock.yaml && npm install --legacy-peer-deps",
  "buildCommand": "npx tsc --noEmit && npx vite build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}